subjects:
  - name: cko.card_processing_settlement_{{ cookiecutter.scheme_slug }}.Charge
    proto: cko.card_processing_settlement_{{ cookiecutter.scheme_slug }}.proto
    ref:
      - name: cko.proto
        version: -1

  - name: cko.card_processing_settlement_{{ cookiecutter.scheme_slug }}.proto
    proto: cko.card_processing_settlement_{{ cookiecutter.scheme_slug }}.proto
    ref:
      - name: cko.proto
        version: -1